<div><form wire:submit="{{ $editingItemId === null ? 'save' : 'update' }}"><input wire:model="part_number" placeholder="Part number"><input wire:model="name" placeholder="Part name"><input wire:model="quantity" type="number" min="0"><button type="submit">{{ $editingItemId === null ? 'Add stock item' : 'Update stock item' }}</button>@if ($editingItemId !== null)<button type="button" wire:click="cancelEdit">Cancel</button>@endif</form>@error('part_number')<p>{{ $message }}</p>@enderror<ul>@forelse($items as $item)<li>{{ $item->part_number }} — {{ $item->name }} ({{ $item->quantity }}) <button type="button" wire:click="edit({{ $item->id }})">Edit</button> <button type="button" wire:click="delete({{ $item->id }})">Delete</button></li>@empty<li>No stock items yet.</li>@endforelse</ul></div>
